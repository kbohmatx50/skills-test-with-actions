name: Testing Workflow Variables

on:
  workflow_dispatch

env:
  cloud: google-cloud

jobs:
  greeting_job:
  runs-on: ubuntu-latest
  env:
    Greeting: Hello
  steps:
    - name: "Say Hello"
    run: echo "$Greeting from $cloud"
    env:
      First_Name: Keith
